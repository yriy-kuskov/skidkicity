# 🏷️ Скидки Города

Современное PWA-приложение для агрегации скидок в магазинах города.

> **📢 Текущий статус:** В активной разработке. 
> Посмотреть ближайшие задачи можно в файле [**TODO.md](./TODO.md).

## 📸 Основные возможности
- **Умный поиск:** Поиск по товарам, магазинам и штрих-кодам с защитой от лишних запросов (Debounce).
- **PWA:** Приложение устанавливается на смартфон/ПК и работает офлайн.
- **Контроль запросов:** Использование `AbortController` для предотвращения ошибок при быстрой смене фильтров.
- **Сканер:** Интерфейс для быстрого добавления скидок через штрих-код.

## 🚀 Технологии
- **Frontend:** React 18, Vite, Tailwind CSS, Lucide React.
- **Backend:** Supabase (Auth, DB, Storage).
- **State:** Кастомные хуки для фильтрации и бесконечного скролла.

## 📦 Установка и запуск
1. `npm install`
2. Создайте `.env` на основе `.env.example`.
3. `npm run dev` — запуск разработки.
4. `npm run build` — сборка PWA.

## 📂 Структура проекта
- `src/lib/api/` — модули взаимодействия с БД (deals.js, products.js).
- `src/hooks/` — бизнес-логика (useDeals, useFilters).
- `src/components/` — UI компоненты, разделенные по смыслу.

## 🛡️ Правила разработки
Проект следует строгим правилам, описанным в `.cursorrules`, включая обязательное использование `;`, одинарных кавычек и Mobile-first подхода.

---
Создано с ❤️ для удобного поиска скидок